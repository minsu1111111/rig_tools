__pycache__/
*.pyc

# Rig-specific state: the golden frame and thresholds belong to one physical
# setup, so they are not portable. Recreate them with rig_check.py on each rig.
# Force-add if you do want yours versioned:  git add -f profiles/
profiles/

# Generated by make_markers.py
markers.png
focus.png
